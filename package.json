{
  "name": "@example/shared",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test",
    "test:ci": "lerna run test:ci",
    "test:html": "lerna run test:html",
    "publish": "lerna publish --conventional-commits --yes --concurrency=1 --exact",
    "publish:git": "yarn run publish --skip-npm",
    "publish:npm": "yarn run publish --skip-git"
  },
  "dependencies": {
    "lerna": "^2.11.0"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  },
  "devDependencies": {
    "cz-conventional-changelog": "^2.1.0"
  }
}
